cat > package.json << 'EOF'
{
  "name": "speedycrm-webhook-service",
  "version": "1.0.0",
  "description": "AI Receptionist Webhook Service for SpeedyCRM",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase-admin": "^11.11.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
EOF